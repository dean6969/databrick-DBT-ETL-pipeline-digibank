version: 2

sources:
  - name: bronze
    database: digibank
    schema: digi_dev_bronze
    quoting:
      database: false
      schema: true
      identifier: true
    tables:
      - name: raw_transaction_history
        identifier: "RAW_transaction_history"

      - name: raw_customer_raw
        identifier: "RAW_customer_raw"

      - name: raw_crm_interactions
        identifier: "RAW_crm_interactions"

      - name: raw_product_enrollments
        identifier: "RAW_product_enrollments"
